import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "rooftop-mood",
  brand: {
    primaryColor: "#FF7A59",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
