import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "olyoung-new",
  brand: {
    primaryColor: "#8F6CFF",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
