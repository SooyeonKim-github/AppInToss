import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "jeomechu",
  brand: {
    primaryColor: "#FF6B4A",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
